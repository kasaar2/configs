[user]
    # Configure your name and email
    name = Kalle
    email = kasaar2@gmail.com

[core]
    excludesfile = ~/.gitignore_global
    editor = vim
    autocrlf = input

[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    lg = log --graph --oneline --decorate --all
    last = log -1 HEAD
    unstage = reset HEAD --
    amend = commit --amend --no-edit

[pull]
    rebase = false

[push]
    default = current
    followTags = true

[init]
    defaultBranch = main

[diff]
    tool = vimdiff
    colorMoved = zebra

[merge]
    conflictstyle = diff3

[color]
    ui = auto

[fetch]
    prune = true

[rebase]
    autoStash = true
